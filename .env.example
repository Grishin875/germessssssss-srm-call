# Обязательно сгенерировать: python -c "import secrets; print(secrets.token_hex(64))"
JWT_SECRET=your-very-secure-random-string-here-min-64-chars

# PostgreSQL
POSTGRES_PASSWORD=password

# Интеграции (опционально)
ONEC_WEBHOOK_SECRET=
BITRIX_WEBHOOK_SECRET=
